<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:reg="http://www.dangdang.com/schema/ddframe/reg"
       xmlns:job="http://www.dangdang.com/schema/ddframe/job"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.springframework.org/schema/context
                        http://www.springframework.org/schema/context/spring-context.xsd
                        http://www.dangdang.com/schema/ddframe/reg
                        http://www.dangdang.com/schema/ddframe/reg/reg.xsd
                        http://www.dangdang.com/schema/ddframe/job
                        http://www.dangdang.com/schema/ddframe/job/job.xsd">

    <context:property-placeholder location="classpath:elastic-job.properties"/>

    <!-- 配置作业注册中心-->
    <reg:zookeeper id="regCenter" server-lists="${job.server.list}"
                   namespace="${job.regCenter.namespace}" base-sleep-time-milliseconds="1000"
                   max-sleep-time-milliseconds="3000" max-retries="3"/>

    <!-- 配置作业-->
    <job:simple id="wechatAccessTokenJob"
                class="com.rookiego.wechat.job.WechatAccessTokenJob"
                registry-center-ref="regCenter" cron="0/10 * * * * ?" overwrite="${job.overwrite}"
                sharding-total-count="1" sharding-item-parameters="0=A">
    </job:simple>
</beans>